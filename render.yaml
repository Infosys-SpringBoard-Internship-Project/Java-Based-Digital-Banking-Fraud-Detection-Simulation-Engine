services:
  - type: web
    name: fraudshield-ml
    env: docker
    rootDir: ./ml
    dockerfilePath: ./Dockerfile
    plan: free
    autoDeploy: true
    healthCheckPath: /health

  - type: web
    name: fraudshield-app
    env: docker
    rootDir: .
    dockerfilePath: ./Dockerfile
    plan: free
    autoDeploy: true
    envVars:
      - key: ML_AUTOTRAIN_ENABLED
        value: "false"
      - key: ML_AUTOTRAIN_INTERVAL_MINUTES
        value: "2880"
    healthCheckPath: /pages/index.html
